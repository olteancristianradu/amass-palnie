# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# producție / build
/build

# secrete & date locale (NU se urcă — fiecare deployment are ale lui)
.env
.env.local
.env*.local
*.db
*.db-journal
/prisma/dev.db
/prisma/*.db

# chei / unelte locale
*.key
*-key.json

# logs / sistem
*.log
.DS_Store
/tmp

# documente interne (rapoarte/analize/design/planuri) — DOAR local, NU pe GitHub.
# Mutate în /Users/radu-server/amass-local-docs/ (separat de aplicația web). Repo-ul e PUBLIC.
/reports/
/docs/
/AUDIT_INCHIDERE.md
/PLAN_PRODUS.md
/PROMPT_CLAUDE_DESIGN.md
/reports/_*.csv
/reports/_*.txt

# build cache TS
tsconfig.tsbuildinfo

# backup-uri locale (NU în git)
*.tgz
dev.db.bak-*
